# Copy this file to .env in each worktree and give each one a different port.
# This is how you run two dev servers at once without "EADDRINUSE" errors.
#
#   worktree A (.worktrees/fix-pricing-bug/.env):  PORT=3000
#   worktree B (.worktrees/fix-date-bug/.env):      PORT=3001
PORT=3000
